
// add, subtract, multiply and divide functions for Calculator
add = (num) => {
  return num + num;
}

subtract = (num) => {
  return num - num;
}

multiply = (num) => {
  return num * num;
}

divide = (num) => {
  return num / num;
}
